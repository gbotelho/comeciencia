$('#form_edit_portion_<%= @portion.id %>').css("display", "none");
$('#form_show_portion_<%= @portion.id %>').css("display", "block");
$('#form_show_portion_<%= @portion.id %>_h6').html("<%= number_with_precision(@portion.size, precision: 0) %> <%= @portion.food.unit %> - <%= number_with_precision((@portion.size * (@portion.food.calories / @portion.food.size)), precision: 0) %> cal"); 
